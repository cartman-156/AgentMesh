Initialize React + TypeScript frontend with shadcn/ui setup.

Requirements:
- Vite or equivalent minimal React setup
- TypeScript enabled
- shadcn/ui configured
- clean folder structure aligned with backend repo layout